export default function Home() {
  return (
    <main className="container py-10">
      <h1 className="text-2xl font-semibold">Influence OS (prototype)</h1>
      <p className="mt-2 text-gray-600">
        Use the nav to explore features. API is at <code>/api/proxy/*</code> and backend at <code>http://localhost:8000/v1</code>.
      </p>
    </main>
  );
}
